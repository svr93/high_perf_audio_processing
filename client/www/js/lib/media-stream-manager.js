/**
 * Module for MediaStream control.
 */
import { AdapterJS } from 'global/web-api';
import {
    SUCCESS_CODE,
    getCodeByStringRepresentation
} from 'lib/error-code-manager';

'use strict';

/**
 * Creates single audio stream.
 * @param {Object=} options
 */
function createStream(options) {}

export {

    createStream
};
